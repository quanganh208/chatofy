# Reserved for the conversation feature - the mobile config layer (src/config/env.ts)
# was removed with the retired local API client; no code reads any variable yet.
# When screens need them, restore the keys (EXPO_PUBLIC_API_BASE_URL, EXPO_PUBLIC_WS_URL,
# EXPO_PUBLIC_ENV) together with a validated config module, not before.
