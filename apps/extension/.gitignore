# WXT build output and generated types
/.output/
/.wxt/

# Copied from @chatofy/realtime-client by scripts/copy-worklets.mjs — the package
# owns the worklet so the extension and the web app cannot drift apart.
/public/worklets/
/e2e/screenshots/
