name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  NODE_VERSION: '24'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client (api)
        run: pnpm --filter api prisma:generate

      # Route through turbo so workspace packages are built (^build) before lint —
      # consumers resolve @chatofy/* from built dist, which is not committed.
      - name: Lint all workspaces
        run: pnpm turbo run lint

  typecheck:
    name: Type check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client (api)
        run: pnpm --filter api prisma:generate

      # Route through turbo so workspace packages are built (^build) before typecheck.
      - name: Type check all workspaces
        run: pnpm turbo run typecheck

  # Lint, typecheck and build all pass on a turn-taking bug that sends one block
  # and hangs, or on a head start that is computed and then silently thrown away
  # — both have shipped from this repo. The suites below are the only gate that
  # can see either, so they run here rather than on whoever remembers.
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client (api)
        run: pnpm --filter api prisma:generate

      # Through turbo so workspace packages are built first: api resolves
      # @chatofy/* from dist, which is not committed.
      - name: Run test suites
        run: pnpm turbo run test

  # The api's e2e suites are NOT covered by the job above: `pnpm turbo run test`
  # reaches api's `test` script, whose jest rootDir is `src`, so nothing under
  # `test/` has ever run in CI. That is where enforcement is proved — the guard,
  # the WebSocket upgrade refusal, the envelope — so it gets its own job.
  api-e2e:
    name: API e2e
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client (api)
        run: pnpm --filter api prisma:generate

      # Workspace packages first: the suites import @chatofy/* from dist. The
      # filter is api's dependency set, not a hand-listed pair — naming packages
      # individually silently omits one the api already imports, and a developer
      # never sees it because their dist directories are left over from an
      # earlier build. Only a clean checkout fails.
      - name: Build workspace packages
        run: pnpm turbo run build --filter=api^...

      # No database: these suites override USER_REPOSITORY with an in-memory
      # implementation, so the real controller, argon2 and JWT issuance all run
      # while only the storage layer is faked.
      - name: Run api e2e suites
        run: pnpm --filter api test:e2e

  # The one suite that needs a real database. Split from the job above so the
  # fast suites are not held behind a service container, and because what this
  # proves cannot be proved without Postgres: the googleSub unique constraint
  # and real findUnique semantics are exactly what the Google linking policy
  # depends on.
  api-e2e-db:
    name: API e2e (Postgres)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: chatofy
          POSTGRES_PASSWORD: chatofy
          POSTGRES_DB: chatofy
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://chatofy:chatofy@localhost:5432/chatofy
      AUTH_JWT_SECRET: ci-only-secret-not-used-by-any-deployment
      # Google's verification is stubbed in the suite; the route still refuses
      # before calling out unless an allowlist exists, so the linking policy is
      # only reachable with this set.
      GOOGLE_CLIENT_IDS: ci-only-google-client-id
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client (api)
        run: pnpm --filter api prisma:generate

      - name: Build workspace packages
        run: pnpm turbo run build --filter=api^...

      - name: Apply migrations
        run: pnpm --filter api exec prisma migrate deploy

      - name: Run the database-backed auth suite
        run: pnpm --filter api test:e2e:db

  build:
    name: Build
    runs-on: ubuntu-latest
    env:
      # `next build` prerenders /login, which reads the server-only config, so
      # the schema must be satisfied for the build to collect page data. A
      # placeholder is correct here and ONLY here: nothing is signed at build
      # time, and the schema stays strict precisely so a real deployment cannot
      # fall back to a default signing secret.
      AUTH_SECRET: ci-build-placeholder-never-signs-anything
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client (api)
        run: pnpm --filter api prisma:generate

      - name: Build all workspaces (Turborepo)
        run: pnpm turbo run build

      # Guards that read a build artifact rather than source. Deliberately in
      # this job and not in Test: `test` depends on `^build` — dependencies'
      # builds, not the package's own — so `.output` does not exist there.
      - name: Verify built artifacts
        run: pnpm turbo run verify:build

  e2e:
    name: Extension e2e
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # The suite loads `.output/chrome-mv3` and does not produce it. Its own
      # staleness guard refuses to run against a bundle older than its source,
      # which is what keeps a green run from being a green run of last week's code.
      - name: Build the extension
        run: pnpm turbo run build --filter=extension

      - name: Install Chromium
        run: pnpm --filter extension exec playwright install --with-deps chromium

      # Until now this suite ran only when someone remembered to, and said so in
      # its own output. It is the only thing standing between the popup rewrite
      # and a silent regression, so "someone remembered" is not a gate.
      - name: Extension e2e
        run: pnpm --filter extension test:e2e

  display-fidelity:
    name: Display ITN gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # The harness loads dist/, not src/ — the same rule the prompt-injection
      # benchmark follows, so a gate cannot pass against a stale build.
      - name: Build workspace packages
        run: pnpm turbo run build --filter=@chatofy/ai-providers

      # The predecessor of this step drove a remote model, spent real quota, and
      # could therefore never run here at all — the display metric was something
      # a person ran once. The ITN needs no key and finishes in milliseconds,
      # which is the only reason these are gates rather than measurements.
      #
      # Held-out NEGATIVES: 50 VIVOS and 50 LibriSpeech utterances the ITN was
      # never built against. Both reference sets carry zero digits, so this
      # cannot score recall — what it does is fail if the ITN ever writes a digit
      # a human has not already reviewed and approved in
      # `display-itn-holdout-approved.jsonl`.
      - name: Held-out negatives (vi)
        run: node benchmarks/stt/scripts/itn_holdout_check.mjs

      - name: Held-out negatives (en)
        run: node benchmarks/stt/scripts/itn_holdout_check.mjs --language en

      # Held-out RECALL, on hand-written sentences verbalized into spoken form.
      # Contains no ASR errors — it measures the grammar, not the pipeline — and
      # it is the only recall evidence English has at all.
      - name: Held-out round-trip recall
        run: node benchmarks/stt/scripts/itn_roundtrip_recall.mjs

  # The replay suites drive the real capture pump and the real turn pipeline over
  # synthesized speech, and their fixtures are not committed — so until now they
  # skipped here, on every run, silently. That is not a theoretical gap: the
  # ordered-playback replay separated its sentences by less than the speech
  # gate's hangover, which meant `maxUtteranceMs` cut every turn to the same
  # length, the server answered in perfect order, and all three of its
  # assertions were satisfiable by a stream that exercised nothing. It read
  # green here throughout. The fixtures are synthesized in this job and the
  # suites are told to fail rather than skip without them.
  realtime-replay:
    name: Realtime replay
    runs-on: ubuntu-latest
    env:
      # Turns "no fixtures" from a skip into a failure — see the replay specs.
      REQUIRE_REALTIME_FIXTURES: '1'
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # ~15MB of synthesized speech that changes only when the generator's
      # sentences or the synthesizer itself changes, so the whole Python and
      # model setup below is skipped on a hit. Restoring this is the difference
      # between a one-minute job and a five-minute one on every pull request.
      - name: Restore synthesized fixtures
        id: fixtures
        uses: actions/cache@v4
        with:
          path: benchmarks/realtime/fixtures
          key: realtime-fixtures-${{ hashFiles('benchmarks/realtime/generate-fixtures.mjs', 'services/local-tts/app.py', 'services/local-tts/engines/*.py', 'services/local-tts/uv.lock') }}

      - name: Install uv
        if: steps.fixtures.outputs.cache-hit != 'true'
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      # Kokoro is ~380MB and VieNeu ~490MB, the latter fetched from Hugging Face
      # by the package itself on first load. Both engines load eagerly at
      # startup, so the English one is needed even though every fixture is
      # Vietnamese.
      - name: Cache the TTS models
        if: steps.fixtures.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: |
            services/local-tts/models
            ~/.cache/huggingface
          key: local-tts-models-${{ hashFiles('services/local-tts/scripts/download_models.py', 'services/local-tts/uv.lock') }}

      - name: Fetch the Kokoro model
        if: steps.fixtures.outputs.cache-hit != 'true'
        run: uv run --directory services/local-tts python scripts/download_models.py

      # `/healthz` answers 503 until both engines are loaded, so it is a real
      # readiness signal rather than a liveness one. VieNeu's cold start alone
      # is ~8s warm and considerably longer on a first model fetch, which is
      # what the ceiling below is sized for.
      # The same missing-SONAME fix `services/local-tts/Dockerfile` applies, and
      # for the same reason: the sherpa-onnx wheel loads an unversioned
      # `libonnxruntime.so` from beside its own libraries, and the onnxruntime
      # wheel ships only the versioned file. Without it the English engine
      # cannot import, and because both engines load eagerly that takes the
      # Vietnamese one — the only one these fixtures need — down with it.
      - name: Link libonnxruntime for sherpa-onnx
        if: steps.fixtures.outputs.cache-hit != 'true'
        run: |
          uv run --directory services/local-tts python - <<'PY'
          import pathlib, onnxruntime

          capi = pathlib.Path(onnxruntime.__file__).parent / "capi"
          site_packages = pathlib.Path(onnxruntime.__file__).parent.parent
          dst = site_packages / "sherpa_onnx.libs" / "libonnxruntime.so"
          if not dst.exists():
              dst.symlink_to(next(capi.glob("libonnxruntime.so.*")))
          print(f"{dst} -> {dst.resolve().name}")
          PY

      - name: Start the TTS sidecar
        if: steps.fixtures.outputs.cache-hit != 'true'
        run: |
          nohup uv run --directory services/local-tts \
            uvicorn app:app --port 8003 > /tmp/local-tts.log 2>&1 &
          sidecar=$!
          for _ in $(seq 1 60); do
            if curl -sf http://127.0.0.1:8003/healthz > /dev/null; then
              echo "sidecar ready"
              exit 0
            fi
            # A startup that raises leaves nothing listening, and waiting the
            # full five minutes for that only delays the log that explains it.
            if ! kill -0 "$sidecar" 2>/dev/null; then
              echo "sidecar exited before becoming ready:"
              cat /tmp/local-tts.log
              exit 1
            fi
            sleep 5
          done
          echo "sidecar never became ready:"
          cat /tmp/local-tts.log
          exit 1

      - name: Synthesize the fixtures
        if: steps.fixtures.outputs.cache-hit != 'true'
        run: node benchmarks/realtime/generate-fixtures.mjs

      # The suites import @chatofy/* from dist, same as every other job here.
      - name: Build workspace packages
        run: pnpm turbo run build --filter=@chatofy/realtime-client^...

      - name: Replay suites
        run: pnpm --filter @chatofy/realtime-client exec vitest run src/audio

  supply-chain:
    name: Supply chain
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # The extension holds tabCapture, scripting and microphone access and runs
      # inside three third-party meeting origins. It is the highest-consequence
      # artifact here, and until now nothing in CI looked at what it pulls in.
      - name: Audit dependencies
        run: pnpm audit --audit-level=high

      # A resolution failure `tsc` cannot see: apps/mobile has no build script,
      # so nothing else in CI ever runs Metro over the shared package.
      - name: One React per bundle
        run: node scripts/check-single-react.mjs
